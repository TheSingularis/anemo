<Window x:Class="NetworkWidget.Core.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Network Widget" Width="220" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="False" ResizeMode="NoResize" Topmost="True"
        WindowStartupLocation="CenterScreen" Background="#202020"
        FontFamily="Segoe UI Variable Text, Segoe UI">
    <Window.Resources>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <Path Data="M0,0 L10,10 M10,0 L0,10"
                                  Stroke="{TemplateBinding Foreground}" StrokeThickness="1"
                                  Width="10" Height="10"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1707A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#3A3A3A" BorderThickness="1">
        <DockPanel LastChildFill="True">
            <Grid DockPanel.Dock="Top" Height="32" Background="#181818" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="txtTitleBar" Grid.Column="0" Text="Network Widget" Foreground="#EEEEEE" FontSize="12"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>
                <Button x:Name="btnClose" Grid.Column="1" Style="{StaticResource CloseButtonStyle}" Click="btnClose_Click"/>
            </Grid>

            <StackPanel Margin="20,24,20,24" HorizontalAlignment="Center">
                <Grid Width="32" Height="32" HorizontalAlignment="Center">
                    <Path x:Name="spinnerArc" Data="M16,3 A13,13 0 1 1 3,16" Stroke="#3A7EE0" StrokeThickness="3"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                          RenderTransformOrigin="0.5,0.5">
                        <Path.RenderTransform>
                            <RotateTransform x:Name="spinnerRotate"/>
                        </Path.RenderTransform>
                    </Path>
                </Grid>

                <TextBlock x:Name="txtStatus" Text="Checking for updates..." Foreground="#EEEEEE" FontSize="12"
                           Margin="0,16,0,0" HorizontalAlignment="Center"/>

                <Border x:Name="progressTrack" Width="160" Height="4" Background="#2B2B2B" CornerRadius="2"
                        Margin="0,12,0,0" HorizontalAlignment="Center" Visibility="Collapsed">
                    <Border x:Name="progressFill" Width="0" Height="4" Background="#3A7EE0" CornerRadius="2"
                            HorizontalAlignment="Left"/>
                </Border>
            </StackPanel>
        </DockPanel>
    </Border>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="spinnerRotate" Storyboard.TargetProperty="Angle"
                                     From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>
</Window>
