<Window x:Class="Anemo.Widget.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Width="280" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="False" ResizeMode="NoResize" Topmost="True"
        WindowStartupLocation="CenterScreen" Background="#202020"
        FontFamily="Segoe UI Variable Text, Segoe UI">
    <Window.Resources>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Arrow"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <Path Data="M0,0 L10,10 M10,0 L0,10"
                                  Stroke="{TemplateBinding Foreground}" StrokeThickness="1"
                                  Width="10" Height="10"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F1707A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#EEEEEE"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Border x:Name="box" Width="18" Height="18" CornerRadius="3"
                                    Background="#2B2B2B" BorderBrush="#4A4A4A" BorderThickness="1" Margin="0,0,10,0">
                                <Path x:Name="check" Data="M3,9 L7,13 L15,4" Stroke="White" StrokeThickness="2"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                      Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="box" Property="Background" Value="#3A7EE0"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="#3A7EE0"/>
                                <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="#6A6A6A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#3A3A3A" BorderThickness="1">
        <DockPanel LastChildFill="True">
            <Grid DockPanel.Dock="Top" Height="32" Background="#181818" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Settings" Foreground="#EEEEEE" FontSize="12"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>
                <Button x:Name="btnClose" Grid.Column="1" Style="{StaticResource CloseButtonStyle}" Click="btnClose_Click"/>
            </Grid>

            <StackPanel Margin="16">
                <CheckBox x:Name="chkStartWithWindows" Content="Start with Windows" Style="{StaticResource ToggleCheckBoxStyle}"
                          Checked="chkStartWithWindows_Changed" Unchecked="chkStartWithWindows_Changed"/>
                <CheckBox x:Name="chkStartMinimized" Content="Start minimized" Style="{StaticResource ToggleCheckBoxStyle}"
                          Checked="chkStartMinimized_Changed" Unchecked="chkStartMinimized_Changed"/>

                <Separator Margin="0,12" Background="#3A3A3A"/>

                <TextBlock x:Name="txtVersion" Text="Version -" Foreground="#666666" FontSize="10"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </DockPanel>
    </Border>
</Window>
